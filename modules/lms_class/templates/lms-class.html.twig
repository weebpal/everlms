{#
/**
 * @file
 *
 * Default template for class item.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - lms_class: The rendered lms_class fields.
 *   Use 'lms_class' to print them all, or print a subset such as
 *   'lms_class.name'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ lms_class|without('name') }}
 *   @endcode
 * - lms_class_entity: The lms_class entity.
 * - lms_class_url: The lms_class URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  {{ lms_class }}
</article>
