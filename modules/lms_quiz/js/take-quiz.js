(function($) {

  $.fn.loadUrlAjaxCallback = function(arguments) {
    // history.pushState({}, '', '/take-quiz/' + (arguments['quiz_id']) + '/question/' + (arguments['question_order'] +1) );// + params.toString())
    // location.reload();
 };
})(jQuery);